<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartSplit">

        <!-- Splash / Entry point -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartSplit.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ChangePasswordActivity" />

        <!-- Main container with BottomNav -->
        <activity android:name=".HomeContainerActivity" />

        <!-- Original activities (still accessible for deep-links / back-compat) -->

        <activity android:name=".GroupsActivity" />

        <!-- Profile -->
        <activity android:name=".EditProfileActivity" />

        <!-- Group flows -->
        <activity android:name=".CreateGroupActivity" />
        <activity android:name=".GroupDetailsActivity" />
        <activity android:name=".AddGroupExpenseActivity" />

        <activity android:name=".SettleUpActivity" />
        <activity android:name=".SettleDetailActivity" />
        <activity android:name=".GroupMembersActivity" />
        <activity android:name=".NotificationsActivity" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="vn.haui.smartsplit.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>